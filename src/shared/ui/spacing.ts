/** Между секциями и блоками страницы (100px → 48px). */
const gapSection = "gap-section";
const ptSection = "pt-section";
const mtSection = "mt-section";

/** Между заголовком секции и контентом (72px → 40px). */
const gapHeading = "gap-heading";

/** Стандартный отступ внутри блока (40px → 24px). */
const gapContent = "gap-content";

export { gapContent, gapHeading, gapSection, mtSection, ptSection };
